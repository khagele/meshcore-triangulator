# Core — required for collector.py, targets.py, locate.py
paho-mqtt>=1.6
numpy>=1.24
scipy>=1.10

# Optional — only needed for terrain-aware mode (locate.py with [terrain] dem_dir set)
# Install separately or uncomment if you use download_dem.py / terrain mode:
# rasterio>=1.3
# msgpack>=1.0   # only for import_meshcore_map.py (map.meshcore.io GPS import)
